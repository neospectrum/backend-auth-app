import { AuthForm } from '../components/AuthForm';
import { Wrapper } from '../components/Skelet/Wrapper';

export const Auth = () => {
    return (
        <div className='login'>
            <AuthForm />
        </div>
    );
};
